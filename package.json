{
  "name": "log-manager",
  "version": "1.0.0",
  "description": "All-in-one logging system for application developers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "echo 'Run individual services: npm run dev:backend and npm run dev:frontend'",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend run preview",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix client-library install",
    "lint": "npm --prefix frontend run lint"
  },
  "keywords": [
    "logging",
    "logger",
    "monitoring",
    "analytics",
    "application-logs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend",
    "client-library"
  ]
}
